# 複製此檔案為 .streamlit/secrets.toml 並填入實際數值（本機開發用）
# 部署到 Streamlit Community Cloud 時，改在後台的 "Secrets" 設定貼上同樣內容，
# 不要把真正的 secrets.toml 上傳到 GitHub（記得加進 .gitignore）

RESEND_API_KEY = "re_你的key"
ALERT_EMAIL_TO = "你的收件信箱"
